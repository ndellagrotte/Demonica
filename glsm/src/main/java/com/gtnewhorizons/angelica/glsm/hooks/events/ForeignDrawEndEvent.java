package com.gtnewhorizons.angelica.glsm.hooks.events;

import net.minecraftforge.eventbus.api.event.MutableEvent;

/** Signals that a nested foreign renderer has returned to the shader pipeline. */
public final class ForeignDrawEndEvent extends MutableEvent {
}
